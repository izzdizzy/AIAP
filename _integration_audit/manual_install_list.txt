# Manual Install List for Original Readmission Backend
# =====================================================
# Run these commands manually to install missing dependencies
# required by the original Hospital Readmission Predictor backend.

# Core Machine Learning Libraries (MISSING)
pip install scikit-learn>=1.3.0
pip install imbalanced-learn>=0.11.0
pip install xgboost>=1.7.0

# Model Interpretability (MISSING)
pip install shap>=0.42.0

# Data Visualization (MISSING)
pip install seaborn>=0.12.0

# Web Application Framework (MISSING - optional, only if running Streamlit standalone)
pip install streamlit>=1.28.0

# Gen AI - Google Gemini (MISSING)
pip install google-generativeai>=0.3.0

# =============================================================================
# SUMMARY OF MISSING PACKAGES:
# =============================================================================
# The following packages are NOT currently installed and MUST be installed:
# 1. scikit-learn    - ML preprocessing and utilities
# 2. imbalanced-learn - SMOTE for handling class imbalance
# 3. xgboost         - Gradient boosting model (required for inference)
# 4. shap            - SHAP values for model interpretability
# 5. seaborn         - Statistical data visualization
# 6. streamlit       - Optional: Only needed for standalone Streamlit app
# 7. google-generativeai - Google Gemini API for chat functionality

# NOTE: The following are already installed:
# - pandas>=2.0.0
# - numpy>=1.24.0
# - joblib>=1.3.0
# - matplotlib>=3.7.0
# - python-dotenv>=1.0.0

# =============================================================================
# AFTER INSTALLATION:
# =============================================================================
# 1. Restart the FastAPI backend:
#    cd /workspace/_integration_workspace/backend
#    uvicorn app:app --reload --host 0.0.0.0 --port 8000
#
# 2. Verify the mounted endpoints are accessible:
#    - GET  http://localhost:8000/readmission/health
#    - POST http://localhost:8000/readmission/api/predict
#    - POST http://localhost:8000/readmission/api/chat
#    - POST http://localhost:8000/readmission/api/upload
#    - GET  http://localhost:8000/readmission/api/model-info
#
# 3. Test the frontend at http://localhost:5173 (or your frontend dev server)
